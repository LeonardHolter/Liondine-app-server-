# NPM Configuration for Railway deployment
# Disable optional dependencies that might cause build issues
optional=false

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Ensure clean install
package-lock=true
